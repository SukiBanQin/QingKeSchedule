# 注意事项

- 每次改动完成后，都必须创建一个对应的 Git commit，以便后续追踪和回滚。
- 每次改动后，都必须编写或更新相关测试，并在交付给用户前，确保所有测试和验证全部通过。

# 项目与实现依据

- `ios/` 是原生 iOS App；`web/` 是视觉与交互 Demo，不能当作完整业务实现。
- `Android/` 放安卓工程，`docs/Android/` 放安卓文档；路径大小写必须一致。
- 安卓目标是完整复现约定 iOS 基准的功能、业务规则和视觉风格。系统权限、文件选择、分享和返回操作采用安卓对应方式，差异必须记录。
- 开始安卓任务先读 `docs/Android/handoff.md`，再按任务读取 `product-baseline.md`、`technical-design.md`、`implementation-plan.md`；这些文件均位于 `docs/Android/`。
- 核对交接记录中的代码基准、当前分支、提交和未提交改动。旧文档与代码不符、发现疑似缺陷或基准变动时，说明差异；不得自行删减功能、重新设计界面或把建议当成用户决定。

# 角色与修改范围

- 分析窗口负责阅读、分析、审查和任务说明，不修改应用代码。用户明确要求写入或更新文档时，可以修改文档及对应验证。
- 执行窗口仅实施已授权任务。讨论方案不等于授权开发，委派子 Agent 也不能绕过角色和任务范围。
- 当前采用官方／中转站各一个分析审查窗口（默认 Sol）和执行窗口（默认 Terra），人工交接；Luna 按需增加，不默认自动委派。
- 派发任务时按 `docs/Android/implementation-plan.md` 建议模型与思考档位并说明理由，由用户在窗口中选择；建议设置不等于已应用设置，不擅自切换模型、档位或服务商。
- 安卓任务默认只修改 `Android/`、`docs/Android/` 及必要的相关验证。涉及 iOS、Web 或共享协议的改动，先说明必要性；没有既有授权时取得确认后再改。
- 保留其他窗口和用户的未提交改动，不擅自覆盖、还原或纳入自己的提交。同一工作区同一时刻只安排一个写入者；确需并行时先隔离工作区并明确提交归属。

# 交接与验证

- 面向用户的总结、交接提示词和新增／更新的说明文档使用中文，技术名称、代码标识和命令保留原文。
- 需要转交时，最终回复末尾必须直接提供唯一一段可完整复制的交接块，并在块外明确接收窗口、建议模型和思考档位。不得只指向文档位置，不让用户找提示词、填写占位符或自行整理提交和测试结果。
- 执行窗口向分析审查窗口回传任务编号、实际提交范围、验证结果及限制和审查请求；分析审查窗口向执行窗口提供具体任务、参考资料、允许修改范围和验收要求。模板见 `docs/Android/implementation-plan.md`，实际发送时由模型填好全部信息，未知项如实标注。
- 模型自行读取相关文档并按职责维护交接；用户只负责复制交接块。需要用户决定时先用通俗中文说明问题，不派发依赖未决事项的任务；无后续任务时明确完成，不制造交接。
- 在重要决定确认、实施阶段结束、审查结束或切换服务前更新 `docs/Android/handoff.md`，不要等额度耗尽才整理。
- 交接写明角色、已确认决定、待审阅建议、已完成与未完成事项、代码基准、相关提交、未提交改动、验证结果和下一步。不要放账号密钥或整段聊天历史。
- 严格区分“已实现”“已测试”“已审查”“用户已验收”。测试未运行或失败必须如实记录，不能声称通过；代码存在或有测试文件不代表已验收。
- 用户反馈“Terra 执行完毕”时，分析窗口先核对交接、提交范围、实际 diff 和验证证据；有问题给修正任务，证据不足列待验证项，审查通过且无阻塞时给下一项任务提示词。不得直接宣布阶段完成或自动开始下一阶段开发。
- 文档修改运行相关文档验证及 `git diff --check`；应用修改运行与变更相符的构建、单元／集成测试，界面和系统能力补充相应 UI／真机验证。受环境限制未完成时记录限制，不声称完整交付。
- 提交前只暂存本任务文件。交接中的代码基准使用已存在的提交；本次文档提交编号由交付消息或 `git log` 查询，避免在文件中自引用尚未产生的提交。
