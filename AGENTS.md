# 注意事项

- 每次改动完成后，都必须创建一个对应的 Git commit，以便后续追踪和回滚。
- 每次改动后，都必须编写或更新相关测试，并在交付给用户前，确保所有测试和验证全部通过。

# 项目与实现依据

- `ios/` 是原生 iOS App；`web/` 是视觉与交互 Demo，不能当作完整业务实现。
- `Android/` 放安卓工程，`docs/Android/` 放安卓文档；路径大小写必须一致。
- 安卓目标是完整复现约定 iOS 基准的功能、业务规则和视觉风格。系统权限、文件选择、分享和返回操作采用安卓对应方式，差异必须记录。
- 开始安卓任务先核对 `docs/Android/handoff.md` 最新状态，再按任务补读 `product-baseline.md`、`technical-design.md`、`implementation-plan.md` 的必要章节；这些文件均位于 `docs/Android/`。已读、未变且上下文仍可用的内容直接复用，遵守下方增量阅读规则。
- 核对交接记录中的代码基准、当前分支、提交和未提交改动。旧文档与代码不符、发现疑似缺陷或基准变动时，说明差异；不得自行删减功能、重新设计界面或把建议当成用户决定。

# 角色与修改范围

- 当前主流程采用一个 Sol 主 Agent 加至多一个 Terra 执行子 Agent。用户主要与主 Agent 对话；主 Agent 负责分析、明确范围、协调、检查实际 diff 和审查，不直接修改应用代码。用户明确授权写入或更新文档时，主 Agent 可以修改文档及对应验证。
- 执行子 Agent 只实施用户已经授权且主 Agent 已明确边界的具体任务；讨论方案、委派行为或已有阶段计划都不构成新阶段授权。不能自动开始未授权阶段。
- 创建执行子 Agent 时必须显式指定 `gpt-5.6-terra` 和思考档位，默认 `medium`；任务已有明确档位约定时保留。为避免静默继承主 Agent 配置，新 Agent 只传必要上下文，不默认继承完整聊天。其他模型或档位调整先向用户说明并由用户决定；工具不支持精确指定时不得用其他模型或普通聊天窗口冒充 Terra。
- 同一时刻最多一个执行子 Agent，并在任务说明中禁止其再创建子 Agent。增加并发子 Agent 数量必须由用户明确决定。
- 原人工执行窗口保留为备用，不删除、不自动发送消息或操作。只有内部子 Agent 不可用、用户明确选择人工交接或切换官方／中转站时，才采用人工执行窗口流程。
- 安卓任务默认只修改 `Android/`、`docs/Android/` 及必要的相关验证。涉及 iOS、Web 或共享协议的改动，先说明必要性；没有既有授权时取得确认后再改。
- 保留其他窗口和用户的未提交改动，不擅自覆盖、还原或纳入自己的提交。共享工作区同一时刻只有一个写入者；Terra 实施时主 Agent 不同时修改文件，只进行不重复实施的独立分析。

# 主从协作与成本控制

- 主 Agent 对已授权且适合委派的具体任务可以自行创建 Terra 执行子 Agent，无需用户逐次复制派发提示词；派发内容仍须覆盖范围、基准、允许修改路径、验收和禁止事项。最终回复只简述目标、进度、结果和需要决定的事项，不重复内部完整任务说明，也不承诺工具调用记录不可见。
- 常规执行子 Agent 固定使用 `gpt-5.6-terra`／`medium`；已有任务明确为 `high` 时使用 `high`。模型或档位变更必须先交给用户决定，不能以失败为由静默切换 Sol、Astra、Luna 或其他模型。
- 同一任务的实施、修正和复审反馈优先复用同一个 Terra 子 Agent。独立任务结束且后续关联较少时，主 Agent 可在保存提交、验证、限制和交接状态后停止旧 Agent，再按需创建新 Agent；不必等整个阶段结束。
- 创建前查找旧 Agent：对同一任务及同阶段高度相关的连续任务，先查询当前主会话的 Agent 列表并核对交接中的标识；列表未显示时，若工具支持且标识属于当前主会话，再尝试续接或恢复。续接指令包含当前基准和授权范围，复用前核对期间变化。不得仅因间隔一两小时、状态为 completed、记忆中没有标识或缓存未命中而新建。
- 创建后在本任务交接中简记 Agent 标识、所属主会话／服务、负责范围、模型／档位和最后一次工具确认的状态；未知字段标注未知，不猜测身份。状态在复用、停止和交接时更新，不写逐轮流水账，不把历史状态当作当前状态。
- 仅在确认旧 Agent 不可访问或无法继续、后续任务关联明显降低，或有明确证据表明上下文不适合当前任务时重建，并简述依据。缓存未命中本身不是重建理由；Agent 可恢复也不代表缓存仍有效。无法确认旧 Agent 是否仍在执行时，先核对运行与写入状态，必要时停止原操作；未确认停止写入前不启动另一个写入者。跨官方／中转站不假定标识可复用。
- 当前工具支持复用已有子 Agent 和中断其正在运行的轮次，但没有永久删除或保证释放 Agent 的操作；停止、关闭、释放或删除状态只按工具实际返回报告，不能把“已中断”写成“已删除”。
- Terra 回报实际提交范围、测试证据和限制；主 Agent 必须核对实际 diff。完整模块、数据迁移、通知、共享协议、工具链变更、用户指定审查及其他关键改动仍保留独立审查，产品验收只由用户确认。
- 切换官方／中转站前先保存交接并停止原写入，不假定子 Agent 会话或工具状态可以跨服务迁移。
- 先观察实际任务的总消耗、返工、重复阅读和交接次数，再调整协作方式；不承诺固定节省额度或比例。
- 小范围、低风险且验证充分的任务可直接交付；完整模块、数据迁移、通知、共享协议、工具链变更及用户明确要求的审查，保留独立审查。任务应写明审查要求和理由；不确定风险时先检查影响范围。P1-03 必须专项复审。
- 正常构建／测试修正不触发升级；问题反复且没有收敛时，整理复现和已尝试方法，再建议升级或交接。遇到未决产品决定，先解决决定。
- 执行子 Agent 可在授权范围内维护本任务交接状态和对应验证；具体任务有更窄授权时遵守该范围。只读当前任务所需资料，保留验证证据，避免无理由重复全量检查或复制整段历史。

# 增量阅读

- 同一窗口已读、未变且上下文仍可用的规则、文档和源码直接复用，不因新一轮消息或交接模板重复全文读取。规则已在当前上下文完整提供时，无需再用工具打印相同内容。
- 接手或准备写入前核对分支、HEAD 和未提交改动；与上次已核实基准比较，仅补读相关 diff、更新章节及必要上下文。Git 提交比较不覆盖未提交改动，不能只看提交号就断言文件未变。
- 新窗口、官方／中转站接手时先恢复当前任务和最新状态，再读取缺少的必要资料，不默认全文读取全部文档。压缩后仅补读缺失细节；无法确认已读内容或版本时，定向重新核对，不凭模糊记忆实施。
- 派发任务列出当前任务入口、相关章节和代码范围；禁止无条件要求“开始前完整阅读”一长串资料。确需全文检查某个文件时说明具体原因；增量阅读不降低验收、实际 diff 审查或必要验证要求。
- 接手模型自行判断需要补读的内容，用户无需记录阅读清单。必要时在交接中简记已核实基准与相关资料范围，不复制全文或维护逐次阅读流水账。
- 本规则减少重复工具输出和上下文增长，不保证缓存命中率或固定节省比例；新子 Agent 接收必要任务上下文，不默认继承完整聊天。

# 交接与验证

- 面向用户的总结、交接提示词和新增／更新的说明文档使用中文，技术名称、代码标识和命令保留原文。
- 内部委派使用子 Agent 工具，不要求用户复制交接块；最终回复不重复内部完整派发提示词。只有实际需要转交人工备用窗口或切换服务时，最终回复末尾才提供唯一一段可完整复制的交接块，并在块外明确接收窗口、建议模型和思考档位。
- 需要独立审查时，执行窗口向分析审查窗口回传任务编号、实际提交范围、验证结果及限制和审查请求；分析审查窗口向执行窗口提供具体任务、参考资料、允许修改范围和验收要求。模板见 `docs/Android/implementation-plan.md`，实际发送时由模型填好全部信息，未知项如实标注。
- 模型自行读取相关文档并按职责维护交接。内部委派无需用户复制；人工备用交接时用户只负责复制唯一交接块。需要用户决定时先用通俗中文说明问题，不派发依赖未决事项的任务；无后续任务时明确完成，不制造交接。
- 在重要决定确认、实施阶段结束、审查结束或切换服务前更新 `docs/Android/handoff.md`，不要等额度耗尽才整理。
- 交接写明角色、已确认决定、待审阅建议、已完成与未完成事项、代码基准、相关提交、未提交改动、验证结果和下一步。不要放账号密钥或整段聊天历史。
- 严格区分“已实现”“已测试”“已审查”“用户已验收”。测试未运行或失败必须如实记录，不能声称通过；代码存在或有测试文件不代表已验收。
- Terra 子 Agent 回报完成时，主 Agent 先核对交接、提交范围、实际 diff 和验证证据；有问题优先复用同一 Terra 修正，证据不足列待验证项。审查通过也只能继续已授权工作，不得直接宣布用户验收或自动开始下一阶段开发。
- 文档修改运行相关文档验证及 `git diff --check`；应用修改运行与变更相符的构建、单元／集成测试，界面和系统能力补充相应 UI／真机验证。受环境限制未完成时记录限制，不声称完整交付。
- 提交前只暂存本任务文件。交接中的代码基准使用已存在的提交；本次文档提交编号由交付消息或 `git log` 查询，避免在文件中自引用尚未产生的提交。

# GitHub 同步

- 正式仓库：[SukiBanQin/QingKeSchedule](https://github.com/SukiBanQin/QingKeSchedule)。`git commit` 只保存本地，`git push` 才上传远程。
- 用户已授权常规交付在相关验证通过并提交后，将当前任务分支推送到该仓库的同名分支，首次推送设置 upstream；不得只提交本地后声称 GitHub 已更新。
- 推送前核对远程地址、当前分支和待上传范围；默认不推送所有分支或标签，不强制推送，不删除远程分支，不自动合并或覆盖 `main`。远程分叉／拒绝时先读取差异并报告，不通过强制推送绕过。
- 推送后核对远程同名分支提交与本地 HEAD 一致，在交付及交接块中写明分支、提交和推送结果。认证／网络失败时明确“已本地提交、未推送”，保留提交供重试。

# 分支与工作区

- `Android` 是安卓开发分支，`IOS` 是 iOS 开发分支，`main` 用于经阶段验收并明确安排合并的稳定成果；分支名大小写固定。
- 安卓分析、实施及相关文档任务统一使用 `Android`；iOS 任务使用 `IOS`。开始任务核对当前分支和未提交改动，分支不符时先确认没有其他窗口正在使用共享目录，再安全切换，不擅自带走或覆盖改动。
- 同一目录切换分支会影响所有窗口；当前安卓窗口共用 `Android` 并串行写入。需要同时推进两端时使用独立 Git worktree，不在共享目录来回切分支。
- 分支包含完整仓库，不能按分支名删除另一平台目录；跨端共享变更需明确安排同步。旧 `codex/ios-ui-redesign-demo` 保留作历史，不再作为日常开发分支。
- 每次交付验证通过后提交并推送对应同名开发分支，报告本地提交编号、GitHub 分支和推送结果。不自动合并 `main`，不把推送成功等同于用户验收。
