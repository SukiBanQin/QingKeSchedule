# P3-04（A04/A05）课程 CRUD 验证证据

日期：2026-09-14

## 范围与基准

- 实现提交：`30015f7 feat(android): implement p3-04 course editor`。
- 本收尾修正提交在本文档写入时尚未产生；交付前由 Git 记录确认。
- 分支：`Android`；仅修改 `Android/` 与 `docs/Android/`。
- 合法重复课程 ID 的编辑／删除按“源课程位置＋打开时 `Course` 指纹”定位；未改 iOS、Web、`source/`、共享 schema、Room schema／版本或导入协议。

## 自动化验证

环境：`ANDROID_HOME` 与 `ANDROID_SDK_ROOT` 均为
`/Users/takagisan/Library/Android/sdk-qingke-api37`，`ANDROID_AVD_HOME` 为
`/Users/takagisan/.android/qingke-api37-r3-avd`，设备为 `qingke-api37-r3-arm` / `emulator-5554`（API 37、`arm64-v8a`）。

| 命令／报告 | 结果 |
| --- | --- |
| `./gradlew testDebugUnitTest testReleaseUnitTest --no-daemon --console=plain` | 通过；Debug／Release 各 8 个 XML，0 failures／errors／skipped。 |
| `./gradlew assembleDebug assembleRelease lintDebug assembleDebugAndroidTest --no-daemon --console=plain` | 通过；Debug、Release、AndroidTest APK 已生成；`lintDebug` 为 0 errors、21 warnings。 |
| `./gradlew connectedDebugAndroidTest --no-daemon --console=plain` | 通过；API 37 ARM64 XML 为 42 tests、0 failures、0 errors、0 skipped。 |
| `git diff --check` | 通过。 |

仪器测试同时覆盖 Room 下重复业务 ID 的精确保存／删除与陈旧指纹拒绝；Compose 回归保留 TODAY 的状态、颜色、详情及 featured 断言。课程行新增可点击语义后，5 处子节点查询改为 `useUnmergedTree = true`，没有降低任何状态、颜色、详情或显示断言。

## 已知验证边界

- 本轮没有把 testhost 截图或固定测试数据称作生产用户验收。
- 生产 APK 的人工新增／追加／编辑／冲突确认／删除／force-stop 重启和浅色／深色／130% 截图仍待 Sol 独立复审时补做；当前不能据此宣称用户验收。

## R1 复审返工（2026-09-14）

- TODAY 在安全区的右下角始终提供真实浮动 `ADD`；无课程仍直达新建，有课程进入按课程名称稳定排序、但保留来源 index 的 chooser。
- 追加模式改为只读复用资料卡；ViewModel 同时拒绝名称、教师、颜色及原始颜色输入的修改。系统返回在确认层存在时只关闭确认层。
- 预设色改为 iOS 六色：`#287B74 #D96952 #536FAF #9A6AAF #B87928 #46835A`；自定义色改为方角 Compose 对话框，原始输入与候选色由 ViewModel 保持。
- 成功反馈改为主壳内 2.6 秒非阻塞终端提示，不使用 Android Toast；编辑器表单修正深色可读性、星期／节次时间文案和 in-flight 禁用语义。
- 新增／扩展 ViewModel、Room、Compose 用例。R1 API 37 ARM64 `connectedDebugAndroidTest`：45 tests、0 failures、0 errors、0 skipped；设备确认 SDK 37、`arm64-v8a`。生产人工长链与截图仍未完成，保持待 Sol 最终复审。

## R2 复审返工（2026-09-14）

- 成功提示正文使用主题对应文字色；Compose 用例验证浅／深主题、主壳 ADD 仍可点击及 2.6 秒自动消失。R2 API 37 ARM64 connected 为 46 tests、0 failures、0 errors、0 skipped。
- 编辑器改为固定品牌头、inverse 顶栏（底部 signal line）、顶部保存、编号资料／安排分区和仅编辑模式可见的 `99 / 危险操作` 删除区；chooser 使用平衡的顶部栏。节次分别显示其对应的起止时间。
- R2 `testReleaseUnitTest assembleDebug assembleRelease lintDebug` 返回 `BUILD SUCCESSFUL`；lint 为 0 errors、21 warnings。生产人工 CRUD 全链和规定截图仍未执行，不能宣称设备生产验收。

## R3 结构收口（2026-09-14）

- 编辑器不再复用 TODAY 的 `LOCAL / 01` 品牌头：顶部工具栏在前，chooser 为 `PROFILE / 04` / `course-choice-brand-header`，编辑器为 `CREATE`、`EDIT` 或 `APPEND / 04` / `course-editor-brand-header`。
- 编号修正为身份 `01`、第一安排 `02`、后续递增、危险区 `99`；开始／结束节次分别显示对应开始／结束时间。新增 Compose 断言覆盖编辑 code、01／02／99 和危险区。
- 本轮未取得生产 UI 长链或截图证据；不得将此结构收口视为该硬门槛完成。

## B 协调层补测（2026-09-14）

- `ScheduleViewModelTest` 新增可执行协调用例：空名称／完全重复不写、冲突确认冻结候选、失败保留草稿、删除取消／成功、重复 ID 第二项精确删除及陈旧指纹拒绝后保留编辑器。Debug JVM 86 tests、0 failures/errors/skipped。
- 本轮只处理 B 自动化，尚未关闭所有 Activity recreate、挂起／取消、Room cancellation 与完整 Compose 流程分支；C 生产证据仍未开始。

## B2 单飞补测（2026-09-14）

- `appendSuccessAndSuspendedSaveDeleteAreSingleFlight` 用 `CompletableDeferred` 实际验证 append 成功文案、挂起保存双击只有一次写入，以及挂起删除双击只有一次写入并在完成后关闭编辑器。Debug JVM 87 tests、0 failures/errors/skipped。
- B 的 Activity recreate／Room cancellation／完整 Compose back 分支仍待补齐，C 仍明确排除。

## B3 编辑器重建与取消补测（2026-09-14）

- `P3R2ActivityRecreationTest.activityRecreationKeepsCourseEditorRouteDraftColorDialogAndConfirmation` 在真实 `scenario.recreate()` 后确认同一 ViewModel 仍保留 CREATE 路由、名称、教师、非法 `colorInput`、颜色对话框、两个安排及放弃确认层；定向 API 37 ARM64 connected 为 3 tests、0 failures/errors/skipped。
- `courseCancellationAndDeleteFailureKeepEditorWithoutOrdinaryError` 验证 save `CancellationException` 后草稿保留、in-flight 恢复且无普通错误，以及 delete 普通失败保留编辑器并发布错误。Debug JVM 88 tests、0 failures/errors/skipped。
- B4 的完整 Compose Back／连续消息和 Room cancellation/reopen 仍未完成；C 仍未开始。

## B4 Compose 与 Room 收口（2026-09-14）

- Compose 增加真实 `onBackPressedDispatcher` 确认层返回、顶部保存／危险删除点击，以及连续 success message 重新计时测试。
- Room 增加 precise delete 普通失败、`CancellationException`、关闭重开及越界／错误指纹后原数据保持断言。AndroidTest APK 编译通过；完整 connected 回归待本轮收尾执行。

## 本轮 connected 测试修正（2026-09-14）

- `RoomScheduleRepositoryTest.preciseDeleteFailureAndCancellationReopenOriginalData` 在 `runBlocking` 结尾显式返回 `Unit`，修正
  JUnit 对表达式体测试方法返回类型的拒绝，使该类全部用例实际进入测试体。
- `QingKeAppTest` 对编辑器 code 和 success message 保留标签可见性断言，并使用未合并语义树的实际 `Text` 节点验证
  `EDIT / 04` 与 `B`。编号分区和危险区先滚动至可见位置，未删除内容验证。
- 连续成功提示测试用 `waitUntil` 保持 Compose 重组可运行，取代阻塞测试线程的 `SystemClock.sleep`；仍验证旧计时器不会提前
  清除 `B`，且 `B` 的 2.6 秒计时会自行结束。
- 三个定向 API 37 ARM64 instrumentation 用例均通过。最终
  `./gradlew connectedDebugAndroidTest --rerun-tasks --no-daemon --console=plain` 的 XML 为 **51 tests、0 failures、0 errors、0 skipped**；
  `./gradlew assembleDebugAndroidTest --no-daemon --console=plain` 与 `git diff --check` 通过。
- 未改生产代码；生产 APK 的人工 CRUD／重启／日志和截图证据仍是未完成硬门槛，Sol 独立复审和用户验收均未完成。

## 编辑器取消文字对比度修正（2026-09-14）

- 生产 APK 截图发现共享 `EditorHeader` 的取消按钮在浅色主题（含 130% font scale）虽然可点击且无障碍树包含文本，但默认
  `OutlinedButton` 取浅色主题 `primary = InverseSurface`，使文字与固定深色 toolbar 同色。
- 取消按钮改为显式 `ButtonDefaults.outlinedButtonColors(contentColor = #F1F5F4)`，匹配 iOS `textOnInverse`；保存按钮原有
  黄底深字色、回调、尺寸和布局未改。
- `editorCloseUsesVisibleInverseInkAcrossThemesAndLargeFont` 在浅色／深色、1.0／1.3 font scale 的四个组合中捕捉按钮位图，
  排除边框后要求实际浅色文字像素存在，并确认按钮显示且可用，避免只凭无障碍文本掩盖同色绘制。
- 定向测试及最终 `./gradlew connectedDebugAndroidTest --rerun-tasks --no-daemon --console=plain` 均通过；最终 XML 为
  **52 tests、0 failures、0 errors、0 skipped**。`lintDebug` 为 0 errors、21 warnings，`assembleDebugAndroidTest` 与
  `testDebugUnitTest`（88 tests、0 failures、0 errors、0 skipped）及 `git diff --check` 通过。生产人工长链与截图仍由 Sol
  继续，未宣称用户验收。
