# P3-03-R2 聚焦视觉修正 API 37 设备证据（2026-09-14）

## 设备恢复与环境

基准为 Android `aa60d94`，本轮在不删除、重建 AVD 或改动工具链的前提下使用：

- `ANDROID_HOME=ANDROID_SDK_ROOT=/Users/takagisan/Library/Android/sdk-qingke-api37`
- `ANDROID_AVD_HOME=/Users/takagisan/.android/qingke-api37-r3-avd`
- AVD：`qingke-api37-r3-arm`；序列号：`emulator-5554`

此前“启动即退出”的判断不准确：`-no-window` 启动命令的父进程很快返回，但 QEMU 实际继续完整冷启动。改以
`-no-snapshot -no-audio -gpu swiftshader_indirect -no-window -verbose` 启动并等待
`sys.boot_completed=1` 后，`adb` 确认 `ro.build.version.sdk=37`、`ro.product.cpu.abi=arm64-v8a`。
关键启动日志保存在本机 `/tmp/qingke-api37-r3-diagnose.log`（未纳入仓库）。

## Connected 测试与截图

最终执行：

```text
cd Android
ANDROID_HOME=... ANDROID_SDK_ROOT=... ANDROID_AVD_HOME=... \
  ./gradlew connectedDebugAndroidTest --no-daemon --console=plain
```

`TEST-qingke-api37-r3-arm(AVD) - 17.xml` 最终为 **41 tests、0 failures、0 errors、0 skipped**。
其中先前一次 41 tests／1 failure 仅暴露新几何测试错误地在合并语义树查询内层标签；修正为
`useUnmergedTree=true` 后重跑通过。该测试现在同时断言内容组宽度小于等宽标签宽度，并断言实际 wrap-content
内容组中心与标签中心误差不超过 1.5px。

本轮 connected 测试宿主重新输出并同步到 `docs/Android/evidence/p3-03-r2/`：

- `android-api37-today-light-testhost.png`、`android-api37-today-dark-testhost.png`：固定课程的浅／深主题，含 featured、课程行及底栏。
- `android-api37-today-font130-testhost.png`：130% 字体；课程行时间缩为固定单行，避免此前在窄时间列换行。
- `android-api37-course-sequence-testhost.png`：固定课程完整队列。
- `android-api37-empty-outside-semester.png`、`android-api37-empty-no-courses.png`、`android-api37-empty-non-teaching.png`：三类空状态。

这些均为 **testhost** 固定课程数据，不能表述为生产入口支持课程录入。生产入口仍只验证到 TODAY 空状态；本轮没有加入 ADD、生产测试数据入口或任何课程 CRUD 功能。

## 视觉自检与平台差异

对浅色、深色和 130% 截图目视核对：裁紧的 Logo 在 154×54 布局框内可读；三个底栏内容组居中，选中块、编号和黄色短线保持方角层级；标准面板和 elevated 底栏均可见左上至右下高光、亮边和阴影，深色表面及阴影更重。130% 下 Logo、hero、featured 倒计时和三标签标题／编号均没有裁切。

与 `ios-fc3ddfb8-today-imported.png` 的既有同状态参考对照，保持 Logo、三段 hero、featured／队列和底栏的结构与方角层级；允许 Android 系统栏、Roboto／Monospace 的字形栅格和 adaptive icon 安全区与 iOS Avenir Next Condensed／安全区不同。

## Sol 独立复审

Sol 主 Agent 已独立核对 `aa60d94..5f5bf2b` 实际 diff、最终 connected XML、设备属性、七张更新截图和 iOS 同状态参考。复审确认：底栏测试使用非合并语义树读取实际 wrap-content 内容组，宽度小于标签且中心误差不超过 1.5px；Logo 三份资源均为 1300×500；standard／elevated 表面分级、独立 `terminalPanelEdge`、高光和阴影没有改变非面板分隔线语义；浅色、深色、130% 与空状态截图未发现新的阻断问题。

结论为 **P3-03-R2 聚焦视觉修正已实施、已设备测试并通过 Sol 技术独立复审，等待用户视觉验收**。这不代表用户已经接受视觉结果，也不代表 P3、A02、A07、A11 或完整 App 已验收。
