P3-05／A03 周课表视觉返修 R2：主机与设备验证记录
日期：2026-09-18    分支：Android    开始基准：af825a1（= origin/Android）    工作区开始时干净

== 环境说明 ================================================================
本会话文件沙箱只允许写入工作区，因此构建使用可写临时目录（不改变仓库配置，也不影响其他窗口）：
  JAVA_HOME=/Applications/Android Studio.app/Contents/jbr/Contents/Home
  GRADLE_USER_HOME=<tmp>/qingke-gradle     ANDROID_USER_HOME=<tmp>/qingke-android-home
  TMPDIR=<tmp>/qingke-tmp                  JAVA_TOOL_OPTIONS=-Djava.io.tmpdir=<tmp>/qingke-tmp -Duser.home=<tmp>/qingke-userhome
  sdk.dir 继续使用 Android/local.properties 的既有配置

== 主机侧 ==================================================================
执行：./gradlew testDebugUnitTest testReleaseUnitTest assembleDebug assembleRelease \
        assembleDebugAndroidTest lintDebug --console=plain --no-daemon
结果：BUILD SUCCESSFUL（144 actionable tasks）
  testDebugUnitTest   tests=92 failures=0 errors=0 skipped=0
  testReleaseUnitTest tests=92 failures=0 errors=0 skipped=0
  lintDebug           errors=0 warnings=20（lint-results-debug.xml 解析）

文档验证（仓库根目录，DEVELOPER_DIR=/Library/Developer/CommandLineTools）：
  python3 docs/tests/android-documentation.test.py  → Ran 70 tests, OK
  bash docs/tests/documentation.test.sh             → Documentation tests passed.
  bash docs/tests/repository-layout.test.sh         → Repository layout tests passed.
  git diff --check                                  → 通过（无输出）

== 设备侧 ==================================================================
设备：emulator-5556，API 37.0 google_apis arm64-v8a，1080x2400，420dpi
完整套件：./gradlew connectedDebugAndroidTest --console=plain --no-daemon（ANDROID_SERIAL=emulator-5556）
结果：BUILD SUCCESSFUL
  TEST-emulator-5556 - 17.xml：tests=74 failures=0 errors=0 skipped=0
  周页用例（7 项，含本轮新增 2 项）：weekControlsKeepAllTextVisibleAndSpacedAcrossFontScales、
    weekControlsRestoreAutoFollowAndManualBrowsingSurvivesClockRefresh、
    weekHeaderControlsAndSectionTitlesFollowIosStructure、
    weekDateStripSelectionMovesSignalUnderlineAndManifestTitleDropsIsoDate、
    weekMatrixDrawsGridLinesAndShowsLunchBreakOnlyWhenEnabled、
    weekDayManifestOmitsIsoDateAndRoutesCourseClicks、
    weekScheduleRendersMatrixHeadersAndRoutesAddAndCourseSource

定向复跑（am instrument）：
  7 项周页用例 → OK (7 tests)
  负向对照：面板 Row 临时改回 R1 的 height(64.dp) → 1 test, Failures: 1
    失败信息：fontScale=1.3 ODD/EVEN WEEK text must be painted inside the panel, cyanPixels=0
  恢复内容高度后同用例 → OK (1 test)

主题独立性修正（本轮发现）：完整套件在本轮执行了 3 次。
  第 1 次（设备 light／1.0）：74 tests、0 failures。
  第 2 次（设备 dark／font_scale 1.3，截图阶段遗留状态）：2 failures——
    weekMatrixDrawsGridLinesAndShowsLunchBreakOnlyWhenEnabled: must draw vertical column lines, lineColumns=0
    weekDateStripSelectionMovesSignalUnderlineAndManifestTitleDropsIsoDate: day strip must draw a top rule, top=90 middle=18
  根因：R1 的像素断言把分隔线写成“比底色更暗”，深色主题下分隔线比底色更亮。
  修正：改为与主题无关的亮度偏差判定 |sample - baseline| >= 12（取样仍限 ±3px）。
  第 3 次（设备 light／1.0）：74 tests、0 failures、0 errors、0 skipped（最终证据 XML）。
  另在 dark／1.3 复跑 7 个周页用例 OK (7 tests)，在 light／1.0 复跑像素用例 OK (3 tests)。

== 生产入口截图核对 ========================================================
数据：全新安装后创建学期「2026 秋季学期」，新增课程 Advanced Mathematics（Zhang，周五第 1 节）
截图（本目录）：light 100%、light 130%、light 130% 日清单、dark 100%、dark 130%
逐张目视结论见本目录 README.md。设备结束时恢复 light／font_scale=1.0；模拟器随后关闭，adb devices 为空。
