# P6／A08 小米 10 真机阶段证据（2026-09-21—22）

状态：**部分验证；A08 尚未完成用户验收，P6 尚未关闭。** 本轮使用小米 10（Android 13／API 33，ADB `15894eb0`）和既有 debug APK；未改应用代码。测试数据为本目录的 `test-schedule-20260921.json`，经系统文件选择器导入，包含当晚 5 门专用测试课程。截图仅用于本次设备设置与通知核对。

## 锁屏测试原布置（历史，2026-09-21 23:35）

再次核对手机时发现用户另行新增了名为 `test` 的课程，因此未再次使用 JSON 整体导入。先把应用私有数据库（含 WAL）和 DataStore 备份到 Mac 临时路径 `/tmp/qingke-a08-mi10-foaZGJ/app-private-20260921.tar`，校验归档结构与 SHA-256；该备份包含设备数据，不进入 Git。随后仅用应用内编辑器新增 `A08LOCK`：2026-09-22 星期二、第 1 周、第 1 节 18:50–18:55、教室 `MI10`。再次读取数据库确认课程与 `test` 并存；提醒登记簿及 `dumpsys alarm` 均指向 **2026-09-22 18:50:00** 的精确 `RTC_WAKEUP`。设备当前提醒偏好为“准时”，本测试不是提前 10 分钟。尚未发生的投递不得记为已通过。

已安排本线程于 2026-09-22 18:35 回来核对设备并观察 18:50 的锁屏投递；若设备断连、被用户使用或测试时间已过，则先报告限制，不擅自改变系统时间或覆盖课表。`A08LOCK` 为本轮新增专用课程，测试结束后与用户确认再清理；原有 `test` 不动。

## 9 月 22 日下午加速验证（进行中）

用户确认小米 10 是备用机，并要求现在直接验证。开始时分支 `Android` 为 `3cb35c1`，只发现无关的未跟踪 `.vscode/`，未触碰。设备仍是 ADB `15894eb0`；先另存含数据库 WAL 与 DataStore 的临时备份 `/tmp/qingke-a08-mi10-foaZGJ/before-time-shift-20260922.tar`（仅 Mac 本地，不入 Git）。

- 提醒关闭／恢复：关闭并保存前，`dumpsys alarm` 列出 `A08LOCK` 的 2026-09-22 18:50 和用户 `test` 的 2026-09-28 22:19 精确 `RTC_WAKEUP`；关闭保存后两条均消失，登记簿只剩 `{"generation":22}`；重新开启并保存后原两条闹钟恢复。没有清空课程数据。
- 删除课程：用正式编辑器新增仅第 1 周、星期二第 2 节的 `A08CANCEL`，系统新增 2026-09-22 19:05 精确闹钟；经编辑器确认删除后，该闹钟消失，数据库中 `A08CANCEL` 不再存在，`A08LOCK` 与用户 `test` 均仍存在。旧时间实际不再投递仍待到点观察。
- 修改课程：新增仅第 1 周、星期二第 3 节的 `A08MOVE`，系统登记 2026-09-22 19:20 精确闹钟；从正式编辑器改到第 4 节后，19:20 闹钟消失，19:35 闹钟出现。旧、新时间的实际投递仍待到点观察。
- 为尽快做锁屏试验，只把第 1 节从 18:50–18:55 调到 **14:25–14:30** 并保存，`A08LOCK` 的精确闹钟随之变为 14:25。用户的 `test` 使用第 6 节，其 22:19–22:54 时间未改。13:55 左右只把青课从小米最近任务中划走、回桌面锁屏；系统闹钟仍在，`dumpsys deviceidle get deep` 为 `ACTIVE`、设备处于充电状态，因此此项只检验锁屏＋清最近任务，**不是 Doze**。到 15:59 仍无这条通知，`dumpsys alarm` 把过时的 14:25 精确闹钟列在 `Pending user blocked background alarms`；青课进程未启动，权限仍授予，不能判定此项通过。
- 16:00 核对小米应用信息：青课“自启动”已开启，省电策略仍为“智能限制后台运行（推荐）”。仅把青课的省电策略改为“无限制”，未改全机策略。随后只将第 1 节改为 **16:25–16:30**，保存后系统登记新的 16:25 精确闹钟；原来被拦的 14:25 闹钟被取消，`Pending user blocked background alarms` 清空。为缩短等待，16:06 再次只把第 1 节改为 **16:12–16:17** 并保存，确认 16:12 精确闹钟登记、16:25 原闹钟取消。16:06:55 仅从最近任务划走青课、回桌面锁屏。16:12 通知实际投递：系统通知记录的标题为 `A08LOCK`，正文为 `16:12–16:17 · MI10`，青课进程被唤起，拦截队列为空；锁屏截图 `15-lockscreen-full-after-unrestricted.png` 清楚显示完整课程名、时间和教室。用户亲自确认“测试我看见了，有震动，并且锁屏页面有通知”。因此**在自启动已开启、青课省电策略改为无限制的条件下，清最近任务＋锁屏投递通过**。该状态不是深度 Doze；不能据此推断默认推荐省电策略也可靠。用户 `test` 所用第 6 节未变。
- 16:18 用户解锁后，仅将第 1 节再次设为 **16:25–16:30**，核对系统精确闹钟。设备原为 AC 充电、100%；测试时执行 `dumpsys battery unplug` 暂时模拟未充电、`dumpsys deviceidle force-idle`，`dumpsys deviceidle get deep` 返回 `IDLE`，之后再次熄屏，`mWakefulness=Dozing`。到 16:24 仍为 `IDLE`／`Dozing`；16:25:30 核对到新通知正文 `16:25–16:30 · MI10`、系统拦截队列为空，证明这条精确提醒在**强制深度 Doze** 中投递。随后执行 `dumpsys deviceidle unforce`、`dumpsys battery reset`，复核深／浅空闲均为 `ACTIVE`、AC 充电恢复且电量 100%。这是开发测试命令强制的深度 Doze，不等同自然长时休眠或所有厂商策略均可靠；用户是否感到震动／看到锁屏待其观察确认。
- 16:28 为测试精确权限降级，临时执行 `appops set com.qingke.schedule SCHEDULE_EXACT_ALARM ignore`。在小米“无限制”省电策略下，16:33 测试闹钟仍显示 `exactAllowReason=allow-listed`，因此这一组合**不能**作为非精确分支证据。仅在测试期间把青课省电策略改回“智能限制后台运行（推荐）”，并在前台把第 1 节改为 **16:40–16:45** 后保存；系统闹钟变为 `window=+6m3s891ms`、`flags=0x20` 的非精确 `RTC_WAKEUP`，另两条未来课程闹钟也为非精确。正式设置页显示“精确闹钟：不可用”“已安排最近 3 条课程提醒（部分可能延迟）”及通知文案将标注“（可能延迟）”，见 `16-inexact-settings.png`。16:41–16:44 闹钟仍在窗口内待投递，约 16:46 系统投递，通知正文为 `16:40–16:45 · MI10 （可能延迟）`，比名义时间晚约 6 分钟。随后将 `SCHEDULE_EXACT_ALARM` app-op 恢复为 `default`，小米青课省电策略恢复“无限制”；复核两条未来课程闹钟均恢复为 `window=0`、`exactAllowReason=permission` 的精确模式，深度空闲为 `ACTIVE`、AC 充电仍在。这里验证的是 ADB 临时模拟的能力不可用路径及实际降级投递，不是用户通过系统设置手动拒绝权限的 UI 流程。

前述“18:35 回来、18:50 提醒”的预约已被加速方案取代；完成当天下午的测试后应停用该旧预约，避免重复打断用户。测试结束还需恢复第 1 节原时间或让用户确认保留，并只清理本轮新增的测试课程，不能动 `test`。

| 检查 | 实际结果与证据 | 结论边界 |
| --- | --- | --- |
| 首次通知权限 | 在应用设置页打开提醒时出现真实系统权限弹窗，见 `01-before-permission.png`、`02-system-permission-prompt.png`；随后页面显示“已授权”，见 `03-system-permission-second-step.png`。 | 已核对弹窗触发与授权后的可用状态；拒绝后继续使用的完整路径本轮未取得可靠证据，不标通过。 |
| 精确投递与文案 | 18:40 对 18:50 课程投递，通知显示课程名、18:50–18:55 和测试教室，见 `04-exact-notification.png`；`05-before-reboot-alarms.txt` 包含后续课程及维护闹钟。 | 已确认通知栏实际投递；初始未观察到悬浮／震动。 |
| 真实重启与小米自启动 | 第一次重启时“自启动”关闭，观察到系统拒绝启动 `ReminderRebuildReceiver`（`process is not permitted to auto start`），重启后无应用闹钟；设置见 `06-autostart-off.png`。开启“自启动”（电池策略仍为“智能限制后台运行”，见 `07-battery-policy-before.png`）并再次真正重启后，无需打开 App，`BOOT_COMPLETED` 启动接收器；闹钟列表恢复 18:55、19:10、19:25、19:40 的课程提醒及维护闹钟，见 `08-*`、`09-*`、`10-*`。 | 小米设备需要允许青课自启动才能完成本次重启恢复；不能把第一次失败当作应用实现缺陷，也不能概括为所有厂商均可自动恢复。 |
| 重启后的通知 | 18:55 的课程提醒已在系统通知栏投递，标题、时间及教室正确，见 `11-reboot-notification-record.txt`。用户最初没在屏幕上看到，展开通知栏后确认看到卡片，并希望有悬浮弹窗或震动。 | 重启后的投递已验证；默认提示观感不符合用户预期。 |
| 厂商通知类别设置与再次观察 | 小米“上课提醒”类别初始为悬浮关闭、声音“无”、振动关闭、锁屏完全不显示，见 `12-channel-defaults.png`。按用户偏好，只对该类别开启悬浮和振动（`13-channel-floating-vibration-on.png`），并在用户明确同意展示完整课程内容后设锁屏“显示通知及其内容”（`14-channel-lockscreen-full.png`）；声音仍为“无”。用户随后亲自确认：“有悬浮通知还有震动了”。 | 悬浮和震动的真机观感获用户确认；锁屏完整内容仅验证了设置值，实际锁屏投递尚未单独确认。用户一度表示“没有看到通知”，随后自行复测确认；不把该前一回复当作最终失败。 |

仍待验证：自然长时休眠（本轮只做了强制深度 Doze 的精确能力分支）；非精确能力下的深度 Doze／厂商长期后台投递；修改／删除课程及关闭提醒后**到旧时间仍不投递**（本轮已证明系统闹钟撤销，但旧时间尚未逐一走到）；真实系统 UI 手动拒绝通知／精确闹钟权限后的完整继续使用路径。小米默认推荐省电策略下清最近任务＋锁屏曾真实拦截，用户需知晓该厂商前提；“无限制”加自启动条件下的短时锁屏投递已通过，但不能推断长期后台投递可靠。

设备临时设置：应用户要求，屏幕自动熄灭从 1 分钟改为 30 分钟，未擅自恢复。小米青课“自启动”、省电策略“无限制”、提醒类别悬浮／振动／锁屏显示仍处于测试后有利于投递的设置；精确闹钟 app-op 已恢复默认且未来系统闹钟为精确模式。测试课程数据仍在此设备，后续收尾时需让用户决定保留还是清除。原定 2026-09-22 18:35 的旧锁屏检查自动预约已删除，不再触发。
