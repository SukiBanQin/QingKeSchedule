# P3-07／A07 教学日历独立技术复审

## 结论

P3-07／A07 在 `Android` 分支的实现范围 `05248f2..53a74d5` 已通过 Sol 独立技术复审，未发现阻断问题或需要返工的应用代码。本结论覆盖实现提交 `6c2096a` 与证据文案修正 `53a74d5`，不等同于用户视觉验收，也不扩大为 A08、A10、A11、整个 P3 或完整 App 验收。

## 审查范围与结果

- 领域规则：`AcademicCalendarResolver` 已从展示层移入 `domain/AcademicCalendarRules.kt`。解析顺序保持为指定停课日 > 指定调课日 > 周末停课 > 正常星期；停课与调课同日互斥，日期去重并排序，同日调课会替换来源星期。调课仍按目标日期所属教学周应用单双周，不移动或删除原星期课程。
- 持久化与写入：ViewModel 的七个教学日历写入入口均通过 `ScheduleAppState.updatePreferences`，在 DataStore 更新转换中基于最新值修改，连续编辑不会以旧快照覆盖其他设置。非法午休范围、失败和取消路径不会写入；有效设置可在重启后恢复。
- 展示联动：首次设置与正式设置共用「03 教学日历」面板；日期、模式、展开状态、调课来源星期、午休草稿和时间选择器 tag 相互独立。今日页与周课表消费同一领域规则，午休只改变周课表展示，不改变课程、节次或停课判断。
- 数据边界：未修改 Room schema、共享 JSON schema 或版本 1，未修改 iOS、Web、`main`。D01 仍维持当前边界：版本 1 备份不携带教学日历，导入不清除本机偏好；如未来改变必须另行决定并同步评估两端协议。
- A08 依赖：领域解析已可供未来通知规划复用，但本次没有实现通知权限、提醒设置、AlarmManager、重建或投递，因此不能宣称今日页、周表与实际通知已经完成一致性验收。

## 独立证据核验

- 复核两个提交的完整差异，共 26 个文件；未发现超出授权范围的 Room、协议、iOS、Web 或 A08／A10／A11 改动。
- 核对既有测试报告：Debug／Release JVM 各 121 tests，设备端 `connectedDebugAndroidTest` 104 tests，均为 0 failures／errors／skipped；`assembleDebug`、`assembleRelease`、`assembleDebugAndroidTest` 通过；`lintDebug` 为 0 errors、20 warnings。
- 核对 9 张截图及节点／主机设备记录。浅色、深色、首次设置、正式设置、停课／调课列表、午休选择器、今日停课、周表午休、窄屏与 130% 字号均有对应证据。
- 130% 字号截图是滚动后的取景，主体控件、文字与按钮没有横向截断，但「03 教学日历」标题上沿位于视口裁切边界；该图不能单独证明标题完整显示。这是证据表述修正，不是已确认的布局缺陷，最终观感仍由用户验收。

## 剩余门槛与限制

- 用户视觉验收尚未进行。程序化截图、节点范围与本次人眼审查不能替代用户在目标设备上的观感确认。
- 切换标签后日历选择态会回到默认值，已经保存的教学日历设置不会丢失；当前作为已知交互限制保留。
- 设备按项目基准 `1080x2400`／420dpi 验证；AVD 原生 `1080x1920` 下四个既有用例因节点裁切失败，执行记录判定并非本任务引入，本次结论不将其表述为原生分辨率验证通过。
- A08、A10、A11 尚未授权实施。没有已授权后续任务，不从本次复审自动派发下一项。
